/**
 * 
 */
package leetcode.array.problems;

/**
 * @author NIPC-57
 *
 *         Date : 23-May-2021 Time : 1:17:39 pm
 */
public class TicTacToe {

	int[][] grid;

	/** Initialize your data structure here. */
	public TicTacToe(int n) {
		grid = new int[n][n];
	}

	/**
	 * Player {player} makes a move at ({row}, {col}).
	 * 
	 * @param row    The row of the board.
	 * @param col    The column of the board.
	 * @param player The player, can be either 1 or 2.
	 * @return The current winning condition, can be either: 0: No one wins. 1:
	 *         Player 1 wins. 2: Player 2 wins.
	 */
	public int move(int row, int col, int player) {

		grid[row][col] = player;

		if (checkIfWon(grid, row, col, player))
			return player;

		return 0;

	}

	private boolean checkIfWon(int[][] grid, int row, int col, int player) {

		// check Vertically and Horizontally
		for (int i = 0; i < grid.length; i++) {
			if (grid[i][col] != player || grid[row][i] != player)
				return false;
		}

		// check Diagonally
		for (int i = 0; i < grid.length; i++) {
			if (grid[i][i] != player)
				return false;
		}

		// check AntiDiagonal
		for (int i = 0, j = grid.length - 1; i < grid.length && j >= 0; i++, j--) {
			if (grid[i][j] != player)
				return false;
		}
		return true;
	}
}
